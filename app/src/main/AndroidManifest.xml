<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mobile_schorgan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_schorgan_foreground"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_schorgan_white_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SobreActivity"></activity>
        <activity android:name=".ui.ocorrencias.OcorrenciasDetailActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".MenuNavActivity"
            android:label="@string/title_activity_menu_nav"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".ui.ocorrencias.OcorrenciaFormularioActivity" />
        <activity android:name=".ui.clientes.ClienteFormularioActivity" />
        <activity android:name=".CadastroUsuarioActivity" />
        <activity android:name=".LoginActivity" />
    </application>

</manifest>